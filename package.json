{
  "name": "jpstripes-connect-2026",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "prepare": "pnpm run cf-build",
    "build:content": "node scripts/build-content.mjs",
    "copy:pdfjs": "node scripts/copy-pdfjs.mjs",
    "cf-build": "pnpm run build:content && pnpm run copy:pdfjs",
    "predev": "pnpm run cf-build",
    "dev": "wrangler dev",
    "prebuild": "pnpm run cf-build",
    "build": "pnpm run cf-build && wrangler deploy --dry-run --outdir dist",
    "predeploy": "pnpm run cf-build",
    "deploy": "wrangler deploy",
    "versions:upload": "pnpm run cf-build && wrangler versions upload",
    "pretest": "pnpm run cf-build",
    "test": "vitest run",
    "test:watch": "vitest",
    "pretypecheck": "pnpm run cf-build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/ua-blocker": "^0.1.35",
    "hono": "^4.11.9",
    "x402-hono": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/sanitize-html": "^2.16.1",
    "gray-matter": "^4.0.3",
    "marked": "^18.0.4",
    "pdfjs-dist": "^4.10.38",
    "sanitize-html": "^2.17.6",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.42.2"
  }
}
